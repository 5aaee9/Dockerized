base {
    log_info = on;
    daemon = on;
    redirector = iptables;
}

redsocks {
    local_ip = 127.0.0.1;
    local_port = 12345;

    ip = {PROXY-SERVER};
    port = {PROXY-PORT};

    type = socks5;
}
