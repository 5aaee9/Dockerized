version: '3'

services:
  mumble:
    image: indexyz/mumble
    build: ./dockerfiles/mumble

  ossutils:
    image: indexyz/ossutils
    build: ./dockerfiles/ossutils

  telegram-send:
    image: indexyz/telegram-send
    build: ./dockerfiles/telegram-send

  php:
    image: indexyz/php
    build: ./dockerfiles/php

  cloudflared:
    image: indexyz/cloudflared
    build: ./dockerfiles/cloudflared

  cfssl:
    image: indexyz/cfssl
    build: ./dockerfiles/cfssl
